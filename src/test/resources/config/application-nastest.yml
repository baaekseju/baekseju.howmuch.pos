spring:
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    hibernate:
      ddl-auto: create
  config:
    import: file:./src/test/resources/config/nas-test-secret.yml

server:
  port: 8000
